# Personal Growth

Автор - __LexCivis__  

## Project's describtion
Архитектура ПО в файле "UML.mdj"

Собрать и запустить, при наличии зависимостей (jre,maven,javaFX) можно с помощью скрипта (написан под Windows) "auto.bat build"

Если программа не открылась нужно запустить команду "assoc .jar=javafile && ftype jarfile=%JAVA_PATH%\javaw.exe -jar "%1" %*" или скрипт "setupJavaSettings.bat", это позволит ассоциировать файлы типа 'jar' с java runtime.
